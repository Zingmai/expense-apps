import 'package:flutter/material.dart';
class FragStatePage extends StatelessWidget {
  const FragStatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue.shade100,
    );
  }
}
